/* const boton = document.getElementById('botonBus');
const inputCli = document.getElementById('search'); */
/* const elemA = document.getElementById('iniciar-sesion-a'); */
//obtener usu
/* let userConnected = JSON.parse(localStorage.getItem(
    "usuarioConectado"
));

if (userConnected) {
    elemA.textContent = 'Cerrar Sesión';
    document.getElementById('prueba').textContent = "prueba";
}

logOut =()=>{
    localStorage.clear();
};

elemA.addEventListener('click', logOut); */


/* const notificar = () => {


    if (inputCli.value == '') {

        Swal.fire({
            'icon': 'warning',
            'title': 'Favor crear su cuenta para hacer búsquedas'
        });
    } else {
        Swal.fire({
            'icon': 'success',
            'title': 'Busqueda exitosa',

        }).then(() => {
            //redireccionar a la pagina de index.html
            window.location.href = 'busqueda.html';
        });
    }
}




boton.addEventListener('click', notificar); */